<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hp.ssm.dao.CommentDao">
    <select id="getShowComments" resultType="com.hp.ssm.model.Comment">
        select id, content, submitid, commenttime, missionid
        from comment
        limit 0,3
    </select>
    <select id="getCommentsBySubmitId" resultType="com.hp.ssm.model.Comment">
        select id, content, submitid, commenttime, missionid
        from comment
        where submitid = #{submitId}
    </select>
    <select id="getCommentsByMissionId" resultType="com.hp.ssm.model.Comment">
        select id, content, submitid, commenttime, missionid
        from comment
        where missionid = #{missionId}
    </select>
    <delete id="deleteCommentById">
        delete
        from comment
        where id = #{id}
    </delete>
    <delete id="deleteCommentsBySubmitId">
        delete
        from comment
        where submitid = #{submitId}
    </delete>
    <insert id="addComment" parameterType="com.hp.ssm.model.Comment">
        insert into comment (content, submitid, commenttime, missionid)
        VALUES (#{content}, #{submitId}, now(), #{missionId})
    </insert>
    <select id="getCommentCountByMissionId" resultType="int">
        select count(id) from comment where missionid=#{missionId}
    </select>
</mapper>
